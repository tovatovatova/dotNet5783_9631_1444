<Window x:Class="PL.Products.ProductItemWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Products"
        mc:Ignorable="d"
              DataContext="{Binding myProduct, RelativeSource={RelativeSource Self}}"

        Title="ProductItemWindow" Height="450" Width="800">
    <Window.Resources>
        <DataTemplate x:Key="ProductItemTemplate" DataType="local:ProductItem">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Border Grid.Row= "0" Width="140" Height="120" CornerRadius="5" Margin="30,50,30,84">
                    <Border.Effect>
                        <DropShadowEffect ShadowDepth="1"/>
                    </Border.Effect>
                    <Border.Background>
                        <ImageBrush ImageSource="{Binding ImagesSource}" />
                    </Border.Background>
                </Border>
                <!--<Image Grid.Row= "0" Height="60" Width="180" HorizontalAlignment="Center" Source="C:\Users\Shilat Sharon\source\repos\ShilatSharon\dotNet5783_1100_2921\PL\zarahome.png" />-->
                <Label  Content="{Binding Name}" Grid.Row= "1" HorizontalAlignment="Center" Margin="5,0,0,0" FontSize="17" />


                <!--<TextBlock Text="out of stock    " HorizontalAlignment="Stretch" Margin="0,5,5,5" VerticalAlignment="Center" Background="LightGray" Grid.ColumnSpan="2" FontFamily="Cooper Black" Foreground="Gray" FontSize="25" Visibility="{Binding InStock , Converter={StaticResource booleanToVisibilityConverter}}">-->
                <!--<TextBlock.LayoutTransform>
                        <RotateTransform Angle="45" />
                    </TextBlock.LayoutTransform>
                </TextBlock>-->
                <TextBlock Text="{Binding Price, StringFormat={}{0:C}}"  Grid.Row= "2" HorizontalAlignment="Center" Margin="5,0,0,0" />
            </Grid>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <TextBlock Text="{Binding Name}" FontWeight="Bold" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="315,261,0,0" Height="67" Width="77" FontSize="39"/>
        <TextBlock Text="{Binding Price,StringFormat={}{0:C}}" FontWeight="Bold" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="315,303,0,0" FontSize="45"/>
        <Image HorizontalAlignment="Left" Source="{Binding ImagesSource}" Height="221" Margin="176,30,0,0" VerticalAlignment="Top" Width="414"/>

        <!--<Grid x:Name="grid1"  HorizontalAlignment="Left" VerticalAlignment="Top" Margin="361,46,0,0" Height="325" Width="384">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="10" />
                <RowDefinition Height="1" />
                <RowDefinition Height="1" />
                <RowDefinition Height="1" />

            </Grid.RowDefinitions>
            <Border Grid.Row= "0" Width="100" Height="100" CornerRadius="5" Margin="0,10,60,124">
                <Border.Effect>
                    <DropShadowEffect ShadowDepth="1"/>
                </Border.Effect>
                <Border.Background>
                    <ImageBrush ImageSource="{Binding ImagesSource}"/>
                </Border.Background>
            </Border>
            <Label  Content="{Binding Name}" HorizontalAlignment="Left" Margin="2" Grid.Row="0" VerticalAlignment="Center"/>
            <Label Grid.Column="0" Content="{Binding Category}" HorizontalAlignment="Left" Margin="2" Grid.Row="1" VerticalAlignment="Center"/>
            <Label Grid.Column="0" Content="{Binding Price, StringFormat={}{0:C}}" HorizontalAlignment="Left" Margin="2" Grid.Row="2" VerticalAlignment="Center"/>
            <CheckBox x:Name="inStockCheckBox" Grid.Column="1" Content="" HorizontalAlignment="Left" IsChecked="{Binding InStock, Mode=TwoWay, NotifyOnValidationError=true, ValidatesOnExceptions=true}" Margin="3" Grid.Row="2" VerticalAlignment="Center"/>
            <Button Content="Add To Cart"  HorizontalAlignment="Left" Margin="450,151,0,0" VerticalAlignment="Top" Height="133" Width="203"  RenderTransformOrigin="0.45,0.235" BorderThickness="1,1,1,1" Grid.RowSpan="5" Click="Button_Click">
                <Button.Background>
                    <ImageBrush/>
                </Button.Background>
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Ellipse Stroke="LightCyan"
                         StrokeThickness="2" Margin="0,0,0,-19">
                                <Ellipse.Fill>
                                    <ImageBrush ImageSource="/Products/1f6739596327dd1b9736589e9fe34802.jpg"/>
                                </Ellipse.Fill>
                            </Ellipse>
                            <ContentPresenter HorizontalAlignment="Center"
                                  VerticalAlignment="Center" OpacityMask="Black"/>
                        </Grid>
                    </ControlTemplate>
                </Button.Template>
            </Button>


        </Grid>
        <Image HorizontalAlignment="Left" Height="183" Margin="10,188,0,0" VerticalAlignment="Top" Width="223"/>-->
        <!--<Image HorizontalAlignment="Left" Source="{Binding ImagesSource}" Height="165" Margin="136,109,0,0" VerticalAlignment="Top" Width="272"/>-->
    </Grid>

</Window>
