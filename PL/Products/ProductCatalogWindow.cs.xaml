<Window x:Class="PL.Products.ProductCatalogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
            DataContext="{Binding myProd, RelativeSource={RelativeSource Self}}"
Title="ProductCatalogWindow" Height="740" Width="1119">
    
    <Window.Resources>
        <DataTemplate x:Key="ProductItemTemplate" DataType="local:ProductItem">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Border Grid.Row= "0" Width="100" Height="100" CornerRadius="5" Margin="30,78,30,84">
                <!--<Border Grid.Row= "0" Width="100" Height="100" CornerRadius="20" Margin="30,50,30,84">-->
                    <Border.Effect>
                        <DropShadowEffect ShadowDepth="1"/>
                    </Border.Effect>
                    <Border.Background>
                        <!--<ImageBrush ImageSource="C:\Users\tovar\source\repos\tovatovatova\dotNet5783_9631_1444\PL\Products\pictures\100003.jpg"/>-->
                        <ImageBrush ImageSource="{Binding ImagesSource}"/>
                    </Border.Background>

                </Border>
                <TextBlock Grid.Row="1"   Text="{Binding Name}" FontSize="15" FontStretch="Normal "/>
                <TextBlock Grid.Row="4" Text="{Binding Category}" FontSize="15" FontStretch="Normal"/>
                <TextBlock Grid.Row="2" Text="{Binding ID}" FontSize="15" FontStretch="Normal" HorizontalAlignment="Center" Margin="5,0,0,0"/>
                <TextBlock Grid.Row="3"  FontSize="15" FontStretch="Normal">
                                            <Run Text="{Binding Price}"/>
                                            <Run Text="$"/>
                </TextBlock>
                <!--<Label  Content="{Binding InStock}" Grid.Row= "1" HorizontalAlignment="Center" Margin="5,0,0,0" FontSize="17" />
                <TextBlock Text="{Binding Price, StringFormat={}{0:C}}"  Grid.Row= "2" HorizontalAlignment="Center" Margin="5,0,0,0" />
                <Label Content="{Binding Categoty}" HorizontalAlignment="Center" Grid.Row= "3" Margin="5,0,0,0"/>-->
                <!--<Label  Content="out of stock" HorizontalAlignment="Center" Grid.Row= "4" Margin="5,0,0,10" VerticalAlignment="Center" Grid.ColumnSpan="2" FontSize="20" Visibility="{Binding InStock, Mode=OneWay}" Background="#FFE9E7E7"/>
                <Button  Content="add to cart" HorizontalAlignment="Center" Height="22" Grid.Row= "5" Margin="5,0,0,10" VerticalAlignment="Top" Width="73" RenderTransformOrigin="0.512,-0.03" IsEnabled="{Binding InStock ,Mode=OneWay}"/>-->
            </Grid>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Image HorizontalAlignment="Left" Height="81" Margin="24,23,0,0" VerticalAlignment="Top" Width="78" Source="C:\Users\tovar\source\repos\tovatovatova\dotNet5783_9631_1444\PL\cart.png.png" RenderTransformOrigin="0.007,0.23"/>
        <ComboBox HorizontalAlignment="Left" Height="29" Margin="953,23,0,0" VerticalAlignment="Top" Width="142"/>
        <!--<Grid Margin="170,174,470,66" RenderTransformOrigin="0.49,0.48">-->
        <!--<Label Content="{Binding numOfItems}" HorizontalAlignment="Left" Height="32" Margin="63,61,0,0" VerticalAlignment="Top" Width="38" RenderTransformOrigin="-1.082,-0.471"/>-->
        <ScrollViewer Background="#FFF1F1F1" Margin="0,113,0,0">

            <StackPanel>
                <TextBlock Text="Items" Margin="10" FontSize="22" FontWeight="Medium"/>
                <StackPanel Margin="20 0">
                    <ListView x:Name="listViewProducts"  ItemTemplate="{StaticResource myProd}" ScrollViewer.HorizontalScrollBarVisibility="Disabled"   MouseDoubleClick="listViewProducts_MouseDoubleClick">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ListView>
                    
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

    </Grid>
</Window>